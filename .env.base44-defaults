# Non-secret defaults for the Base44 compose deployment. Secrets belong in
# /run/base44/app.env (the platform secrets store), which compose loads AFTER
# this file so it wins on conflicts. Safe to commit — do not put keys here.
DATABASE_URL="file:./db/custom.db"
